import React from 'react';
import Index from '../components/tutorial';

export default {
  component: Index,
  title: 'Index',
};

const Template = (args) => <Index {...args} />;

export const Default = Template.bind({});
